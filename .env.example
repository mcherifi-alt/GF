# Auth.js / NextAuth v5
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Domain allowed to sign in — must match the Google Workspace tenant
# already used for HubSpot's own SSO.
ALLOWED_EMAIL_DOMAIN=galienfoundation.org
